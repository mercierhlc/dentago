"use client";

import { SupplierIntegrationsPanel } from "@/components/SupplierIntegrationsPanel";

/** Full-page supplier connections — same UI as Settings → Integrations (embedded=false). */
export default function ClinicSuppliersPage() {
  return <SupplierIntegrationsPanel />;
}
